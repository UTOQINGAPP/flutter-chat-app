import 'package:flutter/material.dart';

class LoadingPageShared extends StatelessWidget {
  const LoadingPageShared({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Loading Page Shared'),
      ),
    );
  }
}
